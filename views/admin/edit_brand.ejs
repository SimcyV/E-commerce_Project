<%- include('../partials/admin_headerTwo') %>

    <div class="container d-flex align-items-center" style="height: 90vh;">
        <div class="row d-flex justify-content-center w-100">
            <div class="col-md-6 p-5 border border-dark rounded">
                <h2 class="text-center pb-4">EDIT BRAND</h2>
                <form action="/editBrand/<%= brandEdit._id %>" method="post" class="form-row">
                    <% if (messages.success) { %>
                        <div class="alert alert-success">
                            <%= messages.success %>
                        </div>
                        <% } else if (messages.error) { %>
                            <div class="alert alert-danger">
                                <%= messages.error %>
                            </div>
                            <%}%>
                                <input type="text" name="name" placeholder="Add new category"
                                    class="form-control border-dark" id="name" value="<%= brandEdit.name %>" required>
                                <p id="nameCheck" class="text-danger"></p>
                                <div class="text-center py-3">
                                    <input type="submit" value="Update" class="btn btn-outline-dark" style="width: 150px;">
                                    <a href="/brandcancel" value="Cancel" class="btn btn-outline-dark"
                                        style="width: 150px;">Cancel</a>
                                </div>
                </form>
            </div>
        </div>
    </div>

<%- include('../partials/footer') %>